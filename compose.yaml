services:
  hub:
    build: .
    ports:
      - "8080:8080"
    environment:
      FERRY_HUB_PASSWORD: ${FERRY_HUB_PASSWORD:?set FERRY_HUB_PASSWORD}
    volumes:
      - ./chats:/data
    restart: unless-stopped
