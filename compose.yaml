services:
  hub:
    build: .
    ports:
      - "8080:8080"
    environment:
      WEAVE_HUB_PASSWORD: ${WEAVE_HUB_PASSWORD:?set WEAVE_HUB_PASSWORD}
    volumes:
      - ./chats:/data
    restart: unless-stopped
